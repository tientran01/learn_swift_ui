import Combine
import Coordinator
import Foundation

class ChatRouter: UIViewControllerCoordinator<ChatRouter.Screen> {
    enum Screen {

    }

    override func transition(for route: Screen) -> ViewTransition {
    }
}
