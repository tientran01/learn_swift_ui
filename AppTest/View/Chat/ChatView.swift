import Combine
import Foundation
import SwiftUI

struct ChatView: View {
    @StateObject var viewModel: ChatViewModel
    let router: ChatRouter

    var body: some View {
        ZStack {
        }
    }
}
