import Combine
import Coordinator
import Foundation

class MainRouter: UIViewControllerCoordinator<MainRouter.Screen> {
    enum Screen {

    }

    override func transition(for route: Screen) -> ViewTransition {
    }
}
