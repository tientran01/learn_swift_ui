import Combine
import Coordinator
import Foundation

class NotificationRouter: UIViewControllerCoordinator<NotificationRouter.Screen> {
    enum Screen {

    }

    override func transition(for route: Screen) -> ViewTransition {
    }
}
