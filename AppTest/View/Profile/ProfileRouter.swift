import Combine
import Coordinator
import Foundation

class ProfileRouter: UIViewControllerCoordinator<ProfileRouter.Screen> {
    enum Screen {

    }

    override func transition(for route: Screen) -> ViewTransition {
    }
}
