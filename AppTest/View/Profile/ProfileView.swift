import Combine
import Foundation
import SwiftUI

struct ProfileView: View {
    @StateObject var viewModel: ProfileViewModel
    let router: ProfileRouter

    var body: some View {
        ZStack {
        }
    }
}
